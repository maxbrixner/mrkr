{% extends "base.jinja" %}

{% block main %}
<div class="error">
    <div class="error-card">
        <h1>We are terrible sorry, but something went wrong!</h1>
        <p>{{status_code}} &mdash; {{error_message}}</p>
        <a class="primary" href="{{url_path_for('projects_page')}}">Return home</a>
    </div>
</div>
{% endblock %}